"""Evaluation harness for did-it (scaffolding).

Design: docs/design/did-it.md — D7 (synthetic corpus = reproducible headline; small execution-labeled
real anchor = private validity cross-check) and "Definition of Done". The published corpus is
fabricated fixtures over throwaway/public repos; the real anchor lives under eval/anchor/ and is
gitignored (never committed).
"""
